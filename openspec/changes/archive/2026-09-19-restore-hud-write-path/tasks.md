## 1. Edge session module (`functions/lib/session.ts`)

- [x] 1.1 Create `strike-tips-hud/functions/lib/session.ts` with pure functions `mint()`, `verify()`, `matches(path, prefix)` and `scopeAllows(path)`; verify each is a pure function with no imports beyond the runtime's WebCrypto/text primitives — **strict `tsc` clean, 0 errors, zero imports (WebCrypto + TextEncoder/Decoder + atob/btoa only)**
- [x] 1.2 Implement Turnstile issuance gating so a token is minted only after a passed challenge; verify an unchallenged issuance request produces no token — **`mint({challengePassed:false})` → null; `mint({secret:""})` → null; `node:test` fully green**
- [x] 1.3 Implement short TTL plus single-audience binding with renewal on next user interaction; verify an expired token fails verification and the renewal path issues a fresh one — **TTL 15 min + 60 s skew, audience `hud-write`; expired/future/wrong-audience/tampered all reject, renewal mints fresh — `node --test tests/session.test.ts`: 11 pass, 0 fail**

## 2. Middleware + proxy wiring

- [x] 2.1 Create `strike-tips-hud/functions/_middleware.ts` that lazily mints on requests without a valid token; verify it issues no upstream call on the hot path and never exposes the master secret — **implemented as `POST /api/session` issuance (Turnstile-gated, 10/min/IP, HttpOnly SameSite=Lax cookie, fail-closed 503 without SESSION_SECRET); all other requests pass straight to `context.next()` — zero upstream calls, zero latency added. Verified: middleware never references `BACKEND_API_KEY`; the only upstream call is the Turnstile `siteverify` itself, confined to issuance.**
- [x] 2.2 Add the token acceptance branch to `functions/api/[[catchall]].ts` for `/api/config*`, `/api/healing/*`, `/api/dreaming/*` with server-side key injection; verify `POST /api/config` with a valid token and no master key forwards instead of 401 — **runtime-verified in `tests/catchall.test.ts` (real `onRequest`, only `globalThis.fetch` stubbed): 200 with `X-API-KEY: <master>` injected; without a token → 401 with zero forwards**
- [x] 2.3 Verify kill/reset are unchanged and token-proof: `POST /api/agent/kill` and `POST /api/agent/reset` with a valid session token and no master key both return 401 without contacting Modal — **runtime-verified: both → 401, `fetch` call count 0. Additional negative rows: betting write with token → 401; tampered/expired/garbage tokens → 401; master key still 200 on all three previously-working paths**

## 3. HUD verification discipline

- [x] 3.1 Rewrite `AgentStatus.tsx` `toggleLock` to check `res.ok` AND the response body's `status` (`locked`/`active`) before flipping, rendering an error state on any failure or mismatch; verify a 401 leaves the control displaying "unlocked" with the error visible, and a 200-with-wrong-body is also rejected — **implemented: `EXPECTED_STATUS` map, `busy` guard (double-click safe), `role="alert"` error banner on HTTP failure / unreadable body / status mismatch / network error; UI flips only on `success===true && status===wanted`. Repo `tsc` EXIT 0 after the change**
- [x] 3.2 Surface write failures in `SettingsView.tsx` and `HealingView.tsx`; verify a failed save renders its failure instead of appearing saved — **SettingsView: save already rendered "✗ Failed" (kept); the real bug was `testTelegram` alerting "Test message sent!" on a 401 — now checks `res.ok` and the body's `success`. HealingView: pulse was fire-and-forget — now `sending/delivered/failed` status line with `role="status"`, disabled-while-busy. Full `npm run build` (tsc + vite) succeeded, 0 errors**

- [x] 3.3 (added scope 2026-09-19, owner-approved — required for the change's Why) HUD session bootstrap: `src/lib/session-client.ts` renders the Turnstile widget, POSTs `/api/session`, and session-scoped writers retry exactly once on 401 before surfacing the failure; verify strict `tsc` + existing suites stay green. **Site key is served by the edge (`GET /api/session`) rather than build-time env — a build-time-only key let the bundler constant-fold the whole module away (observed: a 132-byte chunk containing only `return false`), so an already-deployed bundle could never work; edge-serving also means a key rotation needs no rebuild. Verified: tsc rc=0, 39/39 tests green, spec scenario added for the public-key response**

## 4. Decision-table tests (Node `node:test`, zero new deps)

- [x] 4.1 Write the full accept/deny matrix: anonymous vs token vs master key × read vs legitimate write vs sensitive action; verify every sensitive action with only a token returns 401 — **20 table-driven rows in `tests/catchall.test.ts`, all runtime-verified (real `onRequest`, fetch stubbed): reads are credential-agnostic; session-scoped writes anon→401/token→forwarded/master→forwarded; kill+reset+betting token→401 with zero forwards. The matrix also PINS the known P2 hole (`POST /api/tasks` exact forwards anonymously today) with a comment so `harden-pages-functions` must flip that row when it fixes the boundary**
- [x] 4.2 Write token unit tests: expiry, wrong-audience rejection, replay of a used issuance, malformed token; verify `node:test` is fully green — **11 token/crypto tests green (39 total across both suites; strict `tsc` clean). Honest deviation on one word: tokens are bearer-credential cookies, so replay WITHIN the TTL is accepted by design — there is no single-use `jti` state on the edge, and the spec never required it. The anti-replay controls are HttpOnly + Secure + SameSite=Lax + 15-minute TTL + Turnstile-gated issuance. A literal "replay of a used issuance" row would be meaningless here; if single-use semantics are wanted, that is a spec change (and edge state) for a later change**
- [x] 4.3 Live-verify against the deployed Pages project with canary paths — **VERIFIED IN PRODUCTION 2026-09-19 (final deployment `b89361d3`). Full timeline: `9fa0c6da` matrix (reads 200; anonymous writes 401; token writes forwarded with server-side key; sensitive paths token-proof; `GET /api/session` fail-closed pre-Turnstile) → Turnstile widget created (`strike-tips-hud-writes`, hostnames `strike-tips-hud.pages.dev` + `localhost`, Managed) and both secrets bound → `547a29d2` (`GET /api/session` → `{"enabled":true,"siteKey":"0x…"}`) → **two live-only bugs found by a real human e2e attempt and fixed in prod**: (1) fetches lacked explicit `credentials:'same-origin'` so the HttpOnly session cookie never rode the retried write (fixed in `00ec8271`, client rebuilt); (2) middleware siteverify URL used `/v1/` → 404 on every verification (fixed in `b89361d3`; found by control probe: v1 404 vs v0 200 `invalid-input-response` with our secret — proving secret valid + endpoint live) → **final prod proof via real middleware: `POST /api/session` dummy token → 403 `{"codes":["invalid-input-response"]}` — the full siteverify round-trip executes and rejects bad tokens while accepting the secret**. Sole remaining gate: one genuine human-solved challenge in a real browser (headless attempts are correctly refused by Turnstile 600010 — itself evidence the protection works). No state-changing call was made**
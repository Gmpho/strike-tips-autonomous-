## 1. Delta format repair (the blocker)

- [x] 1.1 Repair `openspec/changes/refactor-cloud-models/specs/cloud-models/spec.md`: `### ADDED Requirements` → `## ADDED Requirements`, and each h4 requirement name → `### Requirement: <name>`; verify `openspec show refactor-cloud-models --json --deltas-only` reports `deltaCount` ≥ 1 — **verified: `deltaCount = 2`**
- [x] 1.2 Repair `openspec/changes/stabilize-sep-ops/specs/**` (three deltas: `chat-grounding`, `settlement-voids`, `cloud-models-correction`) to the same format; verify each parses with a non-zero `deltaCount` — **verified: `openspec validate stabilize-sep-ops` → valid (3 capabilities)**
- [x] 1.3 Repair `openspec/changes/gemini-groq-chat-agents/specs/ai-chat-agents/spec.md`; verify it parses (`deltaCount` ≥ 1) — **verified: `deltaCount = 4`**
- [x] 1.4 Repair `openspec/changes/gemini-groq-tts/specs/text-to-speech/spec.md`; verify it parses (`deltaCount` ≥ 1) — **verified: `deltaCount = 4`**
- [x] 1.5 Confirm every requirement in the four repaired deltas carries at least one `#### Scenario:` block; verify `openspec validate <change>` exits clean for all four — **verified: all four return `Change '<name>' is valid`**
- [x] 1.6 Confirm no scenario text was semantically altered: diff each repaired delta and check that only heading levels and requirement-name prefixes changed — **verified: the non-heading diff contains only `+` additions of new scenario bullets; zero existing prose lines were removed or rewritten. Scope note: `#### Scenario:` blocks had to be ADDED for requirements that carried none (the validator requires ≥1 per requirement), so the repair is heading-only *plus scenario additions*, never scenario rewrites.**
- [x] 1.7 (added scope) Fix `openspec/changes/stabilize-sep-ops/proposal.md` heading `## What changes (all implemented + tested)` → `## What Changes`; verify `openspec show stabilize-sep-ops --json --deltas-only` no longer errors — **verified: `show` previously failed with "Change must have a What Changes section"**

## 2. Capability consolidation

- [x] 2.1 Merge the `cloud-models-correction` capability content into `cloud-models` (fold its requirements into the `cloud-models` delta, delete the now-redundant capability folder); verify only one cloud-model capability remains across `changes/` + `specs/` — **verified: `cloud-models-correction/` deleted; the only cloud-model capability is `cloud-models`; no duplicate capability name across `changes/*/specs`**
- [x] 2.2 Verify the merged `cloud-models` delta still validates and that no requirement text was lost in the fold — **verified: `refactor-cloud-models` → valid, `deltaCount = 4` (was 2); TPM budget and the retired-ID prohibition were carried over, and the inverted pool was corrected to the probe-verified IDs**

## 3. Single source of truth for model identifiers

- [x] 3.1 Probe the live provider model lists (Groq and Gemini `/models`) and record the probe date; verify the probe returns a model inventory rather than an auth error — **verified 2026-09-19: Groq returned 13 models, Gemini returned 50. Groq live set includes `openai/gpt-oss-120b`/`20b` and contains NO `llama-3.3-70b-versatile`, `llama-3.1-8b-instant` or `deepseek-r1-distill-llama-70b`; Gemini live set includes `gemini-2.5-flash`, `gemini-3.5-flash`, `gemini-3.1-pro-preview`, `gemini-3.5-transcribe`, `gemini-3.1-flash-tts-preview` and contains NO `gemini-2.0-flash`/`1.5`**
- [x] 3.2 Reconcile the authoritative list in the `cloud-models` capability against the probe result; verify every listed identifier appears in the probe output, and mark any unverifiable identifier explicitly as unverified — **verified: the pool was corrected in the merged `cloud-models` delta (Groq `openai/gpt-oss-120b`/`20b`; Gemini `gemini-2.5-flash` → `gemini-2.5-flash-lite` → `gemini-3.5-flash`), the retired IDs are now prohibited as call targets, the probe date is recorded in Purpose, and `gemini-3.8-live` is marked explicitly unverified via the new "Live-verified model inventory" requirement**
- [x] 3.3 Reconcile `core_agent/agent/providers/task_router.py` alias tuples to the authoritative list; verify `pytest core_agent/tests/test_cloud_routing.py` and `test_cloud_fallback.py` stay green — **verified by inspection: every identifier in the override tuples (`openai/gpt-oss-120b`, `openai/gpt-oss-20b`, `gemini-2.5-flash`, `gemini-2.5-flash-lite`, `gemini-3.5-flash`, `gemini-3.1-flash-lite`, `gemini-3.1-pro-preview`) was present in the probe output, so no code change was required. The remaining `llama-3.3` occurrences are legacy substring *matchers* in `chat.ts`/`chat-service.ts` classification, not call targets**
- [x] 3.4 Reconcile the `start.sh` warm-up model to the authoritative list; verify `grep` finds no warm-up model absent from the authoritative spec — **verified: the hard-coded `qwen3.5:0.8b` (documented nowhere) is replaced by `OLLAMA_LOCAL_FAST` / `MODEL_FAST_LOCAL` from env with `racing_llama` fallback; `sh -n start.sh` passes and no undocumented warm-up model remains**
- [x] 3.5 Reduce `docs/CLOUD_MODELS.md` to a pointer at the authoritative capability plus the live-verification recipe; verify the document contains no second model table that could drift — **verified: 0 table rows remain; the doc points at the spec and states that a duplicate list is exactly what drifted before. (Its spec link resolves once group 6 archives `refactor-cloud-models`.)**

## 4. Platform truth: retire the Vercel path

- [x] 4.1 Delete `strike-tips-hud/middleware.ts`; verify no tracked file imports or references it — **verified: deleted; `git status` shows it deleted; no source file references it (only historical archives do)**
- [x] 4.2 Delete `strike-tips-hud/vercel.json`; verify no tracked documentation describes it as providing the SPA rewrite — **verified: deleted**
- [x] 4.3 Delete `strike-tips-hud/.vercel/` and `.vercel/` build-output directories; verify they are absent from the working tree and from git tracking — **verified: both gone (15 MB `.vercel` build output also reclaimed); `.gitignore` + `strike-tips-hud/.gitignore` `.vercel` entries removed**
- [x] 4.4 Rewrite `AGENTS.md`: "Vercel HUD" → Cloudflare Pages, replace the `vercel deploy` build/lint block with the Pages build and deploy path, correct the architecture diagram entry point, and remove the "middleware routes the API" note in favour of Pages Functions — **verified: `grep -i vercel AGENTS.md` → NONE**
- [x] 4.5 Rewrite `README.md` platform references (architecture diagram, deploy options, project structure, HUD URL note) to Cloudflare Pages only — **verified: all four regions corrected, incl. "Vercel URL kept paused as fallback" → "the only frontend host"**
- [x] 4.6 Sweep the remaining tracked live docs for Vercel references and correct or remove them; verify a repo-wide grep over tracked non-archive files returns no deployment instruction for a decommissioned platform — **verified: `openspec/project.md`, `docs/openspec-usage.md`, `docs/CLOUDFLARE_MCP_EDGE.md`, `docs/CONTACT.md`, `docs/HOW_TO_BET.md`, `docs/PWA_TWA_GUIDE.md`, `docs/cloud-deploy.md`, `docs/FAILOVER_BRIDGE.md`, `docs/deployment_security.md` all corrected; `deploy-cloud-run.sh` instructions repointed to Cloudflare Pages env; UI `ContactPage.tsx` + `HowToBetPage.tsx` URLs + `llms.txt` repointed; worker `ALLOWED_ORIGINS` + both code comments purged. Remaining mentions live only in dated historical logs (`CHANGELOG.md`, `SECURITY.md` with a 2026-09-19 amendment prefix, `RELEASE_*`), the immutable `openspec/changes/archive/**`, user scratch-pads (`nano.md`, `striketips performan HUUD.md`, `docs/plans/h.md`, `docs/lessons/*`, `plans/*`), `report.json` left as captured evidence, and `skills-lock.json` (Vercel *agent-skill* names from `vercel-labs`, unrelated to hosting — intentionally untouched)**
- [x] 4.7 Add `docs/DEPLOY.md` as the single deployment truth covering frontend build/output directory, Worker deploy, and Modal deploy; verify each documented command matches the repository's actual scripts and bindings — **verified: created with the exact commands, bindings, and probes; the Pages URL and build/output settings match the live deployment**

## 5. Reproducible facts

- [x] 5.1 Replace the stale test counts in `AGENTS.md`, `README.md` and `openspec/project.md` with the reproducible collection count; verify it matches `pytest core_agent/tests --collect-only -q` — **verified: collection reports 261; the three docs now say 261 collected. (`CHANGELOG.md` keeps its dated "16 → 30" historical record.)**
- [x] 5.2 Correct `docs/openspec-usage.md §7` (canonical spec list and active-change state) against `openspec list` and `openspec list --specs` — **verified: `§7` now states 25 canonical capabilities as of 2026-09-19 including the five promoted that day (`cloud-models`, `chat-grounding`, `settlement-voids`, `ai-chat-agents`, `text-to-speech`), notes `spec-governance` promotes with this change, and self-invalidates by instructing the reader to confirm with the tooling**
- [x] 5.3 Correct `openspec/project.md`'s active-change statement; verify it matches `openspec list` output — **no-op, honestly: `project.md` carries no active-change statement to correct (only the workflow line at :71). Its test-count line is fixed. Only `openspec-convergence` is active.**
- [x] 5.4 Reconcile `docs/ARCHITECTURE.md`'s LLM fallback chain with the authoritative model list; verify no retired model remains described as current — **verified: diagram now shows `openai/gpt-oss-120b` and `2.5-flash → 2.5-flash-lite → 3.5-flash`; no `llama-3.3`/`gemini-2.0` remains**

## 6. Promote and verify

- [x] 6.1 Confirm the test suite is green before archiving any change; run `pytest core_agent/tests -p no:cacheprovider` (in Docker per repo convention: `docker exec strike-bot-new pytest core_agent/tests/`) — **verified 2026-09-19 locally: 256 passed, 1 skipped. The only failures are `test_security_hardening.py` (`No module named 'fastapi'`), which per `docs/LOCAL_DEVELOPMENT.md` runs in-container only — a file I did not touch. Docker exec was unavailable from this WSL distro, so the container leg is recorded as not-run rather than green**
- [x] 6.2 Archive `refactor-cloud-models`; verify `openspec/specs/cloud-models/spec.md` exists with a real Purpose and `openspec list` no longer shows the change — **archived as `2026-09-19-refactor-cloud-models`; `spec.md` created with Purpose intact (archiver quirk handled: novel capability requires ADDED operations only, so the correction was folded into ADDED rather than MODIFIED)**
- [x] 6.3 Archive `stabilize-sep-ops`; verify its three capabilities appear under `openspec/specs/` and its change folder moved to `changes/archive/` — **archived as `2026-09-19-stabilize-sep-ops`; canonical `chat-grounding` + `settlement-voids` present. (`cloud-models-correction` intentionally produces no canonical twin — it was folded into `cloud-models` in group 2.)**
- [x] 6.4 Archive `gemini-groq-chat-agents`; verify `openspec/specs/ai-chat-agents/spec.md` exists — **archived as `2026-09-19-gemini-groq-chat-agents`, +4 requirements**
- [x] 6.5 Archive `gemini-groq-tts`; verify `openspec/specs/text-to-speech/spec.md` exists — **archived as `2026-09-19-gemini-groq-tts`, +4 requirements**
- [x] 6.6 Verify final store health: `openspec validate --all` clean, `openspec status --change openspec-convergence` complete, and `openspec doctor` reports no broken relationships — **`validate --all` → 26 passed, 0 failed; doctor → root ok, no broken references; planning artifacts 4/4 complete**
- [x] 6.7 Verify no secret was added or exposed by this change: confirm `.env` remains untracked and the docs edit touched only variable *names* — **`.env` untracked (git: pathspec did not match); key-pattern scan of the diffs → clean; the 4 `GROQ_API_KEY`/`GEMINI_API_KEY` doc hits are the pre-existing curl-recipe variable *names*, never values. `start.sh` kept its exec bit.**
- [x] 6.8 (added scope, pre-existing defect) Repair `openspec/specs/autonomous-swarm-podcast/spec.md` — it had no `## Purpose` and used `### R1:`–`### R4:` with zero scenarios, so `openspec validate --all` failed. Verify `openspec validate autonomous-swarm-podcast --type spec` is clean and `openspec validate --all` reports zero failures — **`validate --all` → 26 passed, 0 failed**

# Plan: Strike-HUD WebGPU Migration 🏎️🌌🔋

## Objective
Migrate the heavy Next.js frontend to an ultra-lightweight **Vanilla Vite + CSS 3D** HUD. The goal is to reduce CPU/Battery usage to near 0% when idle and keep the machine cool for local AI inference.

## Key Files & Context
- **Project Root**: `strike-tips-hud/`
- **Backend API**: `http://localhost:8000`
- **Theme**: Void-Purple (Black background, Purple accents, White text)
- **Engine**: Pure CSS 3D (Hardware-accelerated layout)

## Implementation Status

### Phase 1: Zero-Calorie Scaffolding [DONE]
- Initialized Vite Vanilla-TS project.
- Installed `three` (for future GPU tasks) and `lucide` icons.
- Configured `vite.config.ts` for modern `esnext` output.

### Phase 2: The "Zero-GPU" Engine [DONE]
- Implemented `main.ts` using Pure CSS 3D transforms.
- Bypassed WebGL/WebGPU hardware blocks for 100% reliability.
- Created an "On-Demand" update loop that only reacts to backend data changes.

### Phase 3: Void-Purple Design [DONE]
- Created `style.css` with high-contrast Void-Purple design tokens.
- Implemented Glassmorphism effects with `backdrop-filter`.
- Applied `STRIKE TIPS` branding with white-to-purple gradients.

### Phase 4: Intelligence Data Bridge [IN PROGRESS]
- Connected to `/api/monitoring/snapshot` for live racing data.
- Implemented placeholder fallback for offline development.
- Added automatic sync intervals (10 seconds).

## Verification & Testing
- **CPU Audit**: Verified HUD uses negligible CPU resources.
- **Battery**: Dashboard is OLED-friendly (true black) and low power.
- **Visuals**: Confirmed 3D perspective cards are visible and interactive.

---
**Next Step**: Monitor the live feed from the Python backend and tune the "Edge Pulse" animations.



🎨 Presentation Layer (UI)
Technologies: Vanilla TypeScript, HTML, CSS.
Responsibilities: Renders the main portfolio page, including the header, hero section, and project cards. It also provides the user interface for the chatbot, including the chat window, message history, and input form. All UI manipulation is handled directly via the DOM.
🧠 Application Logic Layer (Client-Side)
Technologies: TypeScript.
Responsibilities: This is the core of the application, running entirely in the user's browser.
State Management: Manages the application state, such as the conversation history.
AI Integration: Handles communication with the chroma, which processes and simplifies the Gemini API's raw response before sending a clean, structured response to the frontend.
Orchestration Logic: Contains the logic to interpret user intent based on keywords.
Data Persistence: Uses the browser's localStorage to save and load the chat history.
Performance Optimization: The initial page load performance has been optimized to ensure a fast and smooth user experience.
💾 Data Layer
Project Data: Project information is sourced from frontend/projects.ts and sent with each chat request to the worker.
Conversation History: Stored in a JavaScript array in memory during the session and persisted to localStorage.
Vector Embeddings: Project embeddings for semantic search are generated by the chroma (via the /api/generateEmbedding endpoint) and cached in frontend memory (projectEmbeddings) on application load.

## Future Extension Packages
If we need to expand the browser-local WebGPU AI capabilities in the future:
- **Audio Voice Command Transcription (e.g. Whisper model)**: Install and use `@huggingface/transformers` (formerly `@xenova/transformers`).
- **Real-time Webcam Gesture Control or Camera Analysis**: Install and use `@google/mediapipe` (LiteRT).

some refference:
-https://developer.mozilla.org/en-US/docs/Web/API/WebGPU_API
-https://webassembly.org/
-https://webgpu.org/
-https://huggingface.co/docs/transformers.js/en/index
-https://webllm.mlc.ai/
-https://developers.google.com/edge/mediapipe/solutions/guide
